# Edge Multi-Port 8-Direction Rendering Fix Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ensure `GraphEdge` always renders the multi-port 8-direction path and 50% arc-length badge coordinates generated by `dagreLayout.ts`, and update `computeEdgePath.ts` to default to 8-direction vector snapping.

---

## Task Breakdown

### Task 1: Update `GraphEdge/index.tsx` and `computeEdgePath.ts`

**Files:**

- Modify: `src/primitives/edges/GraphEdge/index.tsx`
- Modify: `src/primitives/edges/GraphEdge/computeEdgePath.ts`

- [ ] **Step 1: In `GraphEdge/index.tsx`, prioritize `edge.path`, `edge.labelX`, `edge.labelY` calculated by Dagre layout engine**
- [ ] **Step 2: In `computeEdgePath.ts`, update default `pathType` to 8-direction snapped vector path**
- [ ] **Step 3: Run `bun test` to verify all edge tests pass**
- [ ] **Step 4: Run `bun run typecheck && bun run lint && bun run format:check && bun run build:local`**
- [ ] **Step 5: Commit:** `fix: prioritize Dagre multi-port 8-direction edge paths and badges in GraphEdge`
