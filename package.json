{
  "name": "gvui",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "setup": "bash scripts/setup.sh",
    "start": "bun run build && bun run preview:local",
    "start:docker": "bun run prod",
    "dev": "bun run build:wasm && docker compose -f docker-compose.dev.yml up --build --watch",
    "dev:daemon": "bun run build:wasm && docker compose -f docker-compose.dev.yml up -d --build",
    "stop": "docker compose -f docker-compose.dev.yml down",
    "restart": "bun run build:wasm && docker compose -f docker-compose.dev.yml down && docker compose -f docker-compose.dev.yml up --build --watch",
    "restart:daemon": "bun run build:wasm && docker compose -f docker-compose.dev.yml down && docker compose -f docker-compose.dev.yml up -d --build",
    "logs": "docker compose -f docker-compose.dev.yml logs -f",
    "dev:local": "vite --host 0.0.0.0",
    "dev:host": "bun run build:wasm && vite",
    "build:local": "tsc -b && vite build",
    "preview:local": "vite preview",
    "typecheck": "bun run build:wasm && tsc -b",
    "build:wasm": "rm -rf src/engine/layout/custom/wasm_pkg && wasm-pack build crates/gvui --target web --out-dir ../../src/engine/layout/custom/wasm_pkg --no-pack",
    "lint": "oxlint",
    "gvui:import": "bun scripts/import-capsule.ts",
    "format": "oxfmt --write .",
    "format:check": "oxfmt --check",
    "audit": "bun run build:wasm && bun scripts/runLayoutAudit.ts",
    "test": "cargo test --manifest-path crates/gvui/Cargo.toml && bun test src/ && bun run audit",
    "test:visual": "bun scripts/visual-capture.ts",
    "build": "bun run build:wasm && bun run build:local",
    "prod": "bun run build:wasm && docker compose -f docker-compose.prod.yml up -d --build",
    "prod:stop": "docker compose -f docker-compose.prod.yml down",
    "prod:restart": "docker compose -f docker-compose.prod.yml restart",
    "prod:logs": "docker compose -f docker-compose.prod.yml logs -f"
  },
  "dependencies": {
    "@base-ui-components/react": "^1.0.0-rc.0",
    "@tabler/icons-react": "^3.46.0",
    "@tanstack/react-router": "^1.170.18",
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "zustand": "^5.0.14"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "@types/react-test-renderer": "^19.1.0",
    "@vitejs/plugin-react": "^6.0.5",
    "oxfmt": "^0.61.0",
    "oxlint": "^1.76.0",
    "playwright": "^1.62.1",
    "react-test-renderer": "19.2.8",
    "typescript": "^7.0.2",
    "vite": "^8.2.0"
  },
  "engines": {
    "bun": ">=1.0.0"
  }
}
